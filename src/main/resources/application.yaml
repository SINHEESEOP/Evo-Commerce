spring:
  application:
    name: Evo-Commerce

  datasource:
    url: jdbc:mysql://localhost:3306/evo_commerce
    username: root
    password: root1234
    driver-class-name: com.mysql.cj.jdbc.Driver

---
spring:
  config:
    activate:
      on-profile: prod

springdoc:
  api-docs:
    enabled: false
  swagger-ui:
    enabled: false